# RunMe API base URL (no trailing slash).
# Default in code is the Koyeb deploy — you only need this file if you override.

# Local Express API (use when running the server on your machine):
# VITE_SERVER_BASE_URL=http://localhost:4000

# Another deploy:
# VITE_SERVER_BASE_URL=https://your-other-api.example.com
